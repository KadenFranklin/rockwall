@page
@model RockWall.Pages.Spots.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Spot</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Spot.loc" class="control-label"></label>
                <input asp-for="Spot.loc" class="form-control" />
                <span asp-validation-for="Spot.loc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Spot.Position" class="control-label"></label>
                <select asp-for="Spot.Position" class="form-control"></select>
                <span asp-validation-for="Spot.Position" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
